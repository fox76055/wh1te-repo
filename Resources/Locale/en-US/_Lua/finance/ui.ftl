# Finance UI (EN)

# Deposit window row format
finance-deposit-row-format = {$id} | {$principal}+{$accrued} | {$apr}% | next cap ~{$next}s | stop ~{$stop}s | penalty ~{$penalty}

# Deposit amount placeholder
finance-deposit-open-amount-placeholder = Amount ({$min}..{$max})


